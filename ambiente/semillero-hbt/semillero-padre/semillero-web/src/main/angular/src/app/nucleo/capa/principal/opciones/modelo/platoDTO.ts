//Clase que usa los mismos atributos que se encuentra en el archivo Platos DTO de Java
export interface PlatoDTO {
  nombrePlato: string;
  idPlato: number;
  costoPlato: number;
  descripcionPlato: string;
}
